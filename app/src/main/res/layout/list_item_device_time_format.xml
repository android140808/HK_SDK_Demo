<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/iv_time_format"
        android:layout_width="97dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:src="@mipmap/device_time_format_h_1" />

    <ImageView
        android:id="@+id/iv_selected"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:src="@mipmap/select_device2" />

    <TextView
        android:id="@+id/tv_time_format"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="6dp"
        android:layout_toLeftOf="@+id/iv_selected"
        android:layout_toRightOf="@id/iv_time_format"
        android:gravity="center"
        android:text="VERTICAL\nTIME+DAY+BATTERY+HEART RATE\n12-H"
        android:textColor="@color/black"
        android:textSize="15sp" />
</RelativeLayout>